services:
  # Demo Script (Consola)
  kairos-demo:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: kairos-demo
    volumes:
      - ./src:/app/src:z
      - ./data:/app/data:z
      - ./config:/app/config:z
      - ./scripts:/app/scripts:z
    environment:
      - KAIROS_ENV=demo
      - PYTHONPATH=/app/src
    # Corre el script de validacion con datos reales de UADE
    command: ["python", "scripts/validar_motor_real.py"]
